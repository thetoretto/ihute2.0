# The app uses the real API by default. Set EXPO_PUBLIC_API_BASE_URL to your backend.
# Set EXPO_PUBLIC_USE_REAL_API=false only for local development with in-app mocks (no server).
# EXPO_PUBLIC_USE_REAL_API=false

# Base URL of the API server (required when using real API). Examples:
# - iOS Simulator / web: http://localhost:3000
# - Android emulator: http://10.0.2.2:3000
# - Physical device (same Wi-Fi as your PC): http://YOUR_PC_IP:3000
# EXPO_PUBLIC_API_BASE_URL=http://localhost:3000

# WhatsApp number for dispute/support (digits only, e.g. 250788123456). User is taken to this number when they tap "Dispute via WhatsApp".
# EXPO_PUBLIC_DISPUTE_WHATSAPP_NUMBER=250788123456
