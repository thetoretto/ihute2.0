# Set to true to use the local API server instead of in-app mocks.
# When enabled, use the same server URL as admin-web and landing-page (see docs/API.md).
# EXPO_PUBLIC_USE_REAL_API=true

# Base URL of the API server. Examples:
# - Emulator: http://localhost:3000
# - Android emulator: http://10.0.2.2:3000
# - Physical device (replace with your PC's IP): http://192.168.1.100:3000
# EXPO_PUBLIC_API_BASE_URL=http://localhost:3000

# WhatsApp number for dispute/support (digits only, e.g. 250788123456). User is taken to this number when they tap "Dispute via WhatsApp".
# EXPO_PUBLIC_DISPUTE_WHATSAPP_NUMBER=250788123456
