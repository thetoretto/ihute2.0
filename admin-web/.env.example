# Base URL of the backend. When set, admin-web loads hotpoints, trips, bookings, vehicles, users, and disputes from the backend (same backend as mobile and landing-page).
# Example: http://localhost:3000
# VITE_API_BASE_URL=http://localhost:3000
