# Base URL of the API server. When set, landing-page fetches trips and instant queue from the server and creates bookings there (same server as mobile and admin-web).
# Example: http://localhost:3000
# VITE_API_BASE_URL=http://localhost:3000

# Optional: passenger user id used when creating a booking from the landing page (default: u_passenger_1)
# VITE_BOOKING_PASSENGER_ID=u_passenger_1
