# Railway config for ihute landing page (landing-page).
# In Railway dashboard, set this service's Root Directory to "landing-page".
# Set env var VITE_API_BASE_URL to your deployed API URL (e.g. https://your-api.up.railway.app).

[build]
buildCommand = "npm install && npm run build"

[deploy]
startCommand = "npm start"
