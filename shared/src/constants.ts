/**
 * Shared constants for IDs. Backend should use the same booking ID prefix when integrated.
 */

/** Prefix for booking IDs. Backend should use the same when generating IDs. */
export const BOOKING_ID_PREFIX = 'b_';
